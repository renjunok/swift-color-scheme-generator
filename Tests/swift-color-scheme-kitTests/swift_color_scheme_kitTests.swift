import XCTest
@testable import swift_color_scheme_kit

final class swift_color_scheme_kitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_color_scheme_kit().text, "Hello, World!")
    }
}
